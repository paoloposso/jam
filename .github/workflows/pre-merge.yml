name: Pre-merge

on:
  pull_request:
    branches:
      - main
      - dev

jobs:
  trigger_workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Build and Test Workflow
        uses: octo-org/workflow-call-action@v1
        with:
          workflow: Build and Test
